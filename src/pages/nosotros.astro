---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// Traer trabajos entregados (ultimos 10)
const allTrabajos = (await getCollection('trabajos')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
const trabajos = allTrabajos.slice(0, 10);

// KPIs
const currentYear = new Date().getFullYear();
const BASE_PROYECTOS = 36; // proyectos historicos antes del sistema
const BASE_CLIENTES = 24; // clientes historicos antes del sistema

const trabajosEsteAno = BASE_PROYECTOS + allTrabajos.filter(t => t.data.date.getFullYear() === currentYear).length;

// Kg procesados: base historica + suma real de pesoKg de proyectos del sistema
// Base: ~45 sets parrilla+frente (50kg + 15kg = 65kg × 45 = 2925 ≈ 3000 kg)
const BASE_KG = 3000;
const kgSistema = allTrabajos.reduce((sum, t) => sum + (t.data.pesoKg || 0), 0);
const kgTotal = BASE_KG + kgSistema;

// Clientes satisfechos: base + clientes unicos del sistema
const clientesSistema = new Set(
  allTrabajos.filter(t => t.data.cliente).map(t => t.data.cliente.toLowerCase().trim())
);
const totalClientes = BASE_CLIENTES + clientesSistema.size;
---

<BaseLayout
  title="Nosotros - Nuestra Historia"
  description="Conocé la historia de Metalurgica Boto Mariani. Más de 30 años de experiencia en fabricación de estructuras metálicas, gabinetes eléctricos y arquitectura metálica."
>
  <!-- Hero -->
  <section class="relative bg-primary text-white py-20">
    <div class="container">
      <div class="max-w-3xl">
        <h1 class="text-4xl md:text-5xl font-bold leading-tight mb-6">
          NUESTRA<br />
          <span class="text-accent">HISTORIA</span>
        </h1>
        <p class="text-xl text-gray-200">
          Más de 30 años fabricando soluciones en metal para la industria argentina.
        </p>
      </div>
    </div>
  </section>

  <!-- About -->
  <section class="section">
    <div class="container">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <h2 class="section-title">Metalurgica Boto Mariani</h2>
          <p class="text-gray-600 mb-6">
            Desde hace más de tres décadas, nos dedicamos a la fabricación y construcción de infraestructura metalmecánica, brindando soluciones integrales para la industria, la construcción y el hogar.
          </p>
          <p class="text-gray-600 mb-6">
            Nuestro compromiso con la calidad nos ha llevado a obtener certificaciones INTI para nuestra línea de gabinetes y shelters, convirtiéndonos en proveedores de confianza para empresas líderes como YPF.
          </p>
          <p class="text-gray-600">
            Contamos con instalaciones propias equipadas con tecnología de corte láser, plasma y plegadoras de última generación, lo que nos permite ofrecer productos de la más alta calidad y precisión.
          </p>
        </div>
        <div class="flex items-center justify-center">
          <img
            src="/logos/logo.png"
            alt="Metalurgica Boto Mariani"
            class="w-full max-w-sm"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Values -->
  <section class="section bg-gray-50">
    <div class="container">
      <div class="text-center mb-12">
        <h2 class="section-title">Nuestros Valores</h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="bg-white p-8 rounded-lg text-center">
          <div class="w-16 h-16 bg-primary rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Calidad</h3>
          <p class="text-gray-600">
            Estrictos controles de calidad en cada etapa del proceso productivo, respaldados por certificaciones INTI.
          </p>
        </div>

        <div class="bg-white p-8 rounded-lg text-center">
          <div class="w-16 h-16 bg-primary rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Atención Personalizada</h3>
          <p class="text-gray-600">
            Cada proyecto es único. Brindamos asesoramiento técnico especializado para encontrar la mejor solución.
          </p>
        </div>

        <div class="bg-white p-8 rounded-lg text-center">
          <div class="w-16 h-16 bg-primary rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-3">Cumplimiento</h3>
          <p class="text-gray-600">
            Entrega efectiva en tiempo y forma. Nuestro compromiso es cumplir con lo acordado en cada proyecto.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Capabilities -->
  <section class="section">
    <div class="container">
      <div class="text-center mb-12">
        <h2 class="section-title">Capacidad Productiva</h2>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
        <div class="text-center p-6 bg-gray-50 rounded-lg">
          <svg class="w-10 h-10 text-primary mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
          </svg>
          <h4 class="font-bold text-gray-900">Corte Láser</h4>
          <p class="text-sm text-gray-500">Alta precisión</p>
        </div>

        <div class="text-center p-6 bg-gray-50 rounded-lg">
          <svg class="w-10 h-10 text-primary mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
          <h4 class="font-bold text-gray-900">Corte Plasma</h4>
          <p class="text-sm text-gray-500">Grandes espesores</p>
        </div>

        <div class="text-center p-6 bg-gray-50 rounded-lg">
          <svg class="w-10 h-10 text-primary mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6z" />
          </svg>
          <h4 class="font-bold text-gray-900">Plegado CNC</h4>
          <p class="text-sm text-gray-500">Precisión milimétrica</p>
        </div>

        <div class="text-center p-6 bg-gray-50 rounded-lg">
          <svg class="w-10 h-10 text-primary mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z" />
          </svg>
          <h4 class="font-bold text-gray-900">Soldadura</h4>
          <p class="text-sm text-gray-500">MIG/TIG/Electrodo</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Trabajos Entregados -->
  <section id="trabajos" class="section bg-gray-50">
    <div class="container">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">Trabajos Entregados</h2>
        <p class="text-gray-600 max-w-2xl mx-auto">
          Algunos de nuestros proyectos mas recientes. Cada trabajo refleja nuestro compromiso con la calidad y la precision.
        </p>
      </div>

      <!-- KPIs -->
      <div class="grid grid-cols-3 gap-6 mb-12 max-w-2xl mx-auto">
        <div class="text-center bg-white p-6 rounded-xl shadow-sm">
          <div class="text-3xl md:text-4xl font-bold text-primary">+{trabajosEsteAno}</div>
          <div class="text-sm text-gray-500 mt-1">Proyectos entregados</div>
        </div>
        <div class="text-center bg-white p-6 rounded-xl shadow-sm">
          <div class="text-3xl md:text-4xl font-bold text-primary">{kgTotal.toLocaleString('es-AR')}</div>
          <div class="text-sm text-gray-500 mt-1">Kg procesados</div>
        </div>
        <div class="text-center bg-white p-6 rounded-xl shadow-sm">
          <div class="text-3xl md:text-4xl font-bold text-accent">+{totalClientes}</div>
          <div class="text-sm text-gray-500 mt-1">Clientes satisfechos</div>
        </div>
      </div>

      {trabajos.length > 0 ? (
        <div class="flex flex-col gap-10">
          {trabajos.map((t) => {
            const fotos = t.data.images?.length > 0 ? t.data.images : [t.data.image];
            return (
              <div class="bg-white rounded-xl shadow-sm overflow-hidden">
                <div class="p-5 pb-3">
                  <div class="flex items-center gap-3 mb-2">
                    <span class="inline-block bg-accent text-xs font-bold px-2.5 py-1 rounded text-gray-900">{t.data.category}</span>
                    <span class="text-xs text-gray-400">{t.data.date.toLocaleDateString('es-AR', { day: 'numeric', month: 'short', year: 'numeric' })}</span>
                  </div>
                  <h3 class="text-lg font-bold text-gray-900 mb-1">{t.data.title}</h3>
                  <p class="text-sm text-gray-600">{t.data.description}</p>
                </div>
                <div class="trabajo-carousel flex gap-3 overflow-x-auto px-5 pb-5 scroll-smooth" style="scroll-snap-type: x mandatory; -webkit-overflow-scrolling: touch;">
                  {fotos.map((img: string, i: number) => (
                    <div class="flex-shrink-0 rounded-lg overflow-hidden" style="scroll-snap-align: start; width: 280px; height: 200px;">
                      <img
                        src={img}
                        alt={`${t.data.title} - foto ${i + 1}`}
                        class="w-full h-full object-cover"
                        loading="lazy"
                      />
                    </div>
                  ))}
                </div>
              </div>
            );
          })}
        </div>
      ) : (
        <div class="text-center py-12 text-gray-400">
          <p>Proximamente publicaremos nuestros trabajos mas recientes.</p>
        </div>
      )}
    </div>
  </section>

  <!-- CTA -->
  <section class="section bg-primary text-white">
    <div class="container text-center">
      <h2 class="text-3xl font-bold mb-4">¿Listo para trabajar juntos?</h2>
      <p class="text-lg text-white/80 mb-8 max-w-2xl mx-auto">
        Contactanos y descubrí cómo podemos ayudarte con tu próximo proyecto.
      </p>
      <a href="/contacto/" class="btn btn-accent">
        Contactar
      </a>
    </div>
  </section>
  <style>
    .trabajo-carousel::-webkit-scrollbar { display: none; }
    .trabajo-carousel { scrollbar-width: none; }
  </style>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const carousels = document.querySelectorAll<HTMLDivElement>('.trabajo-carousel');
      carousels.forEach(c => {
        if (c.children.length <= 1) return;
        const itemW = 280 + 12; // width + gap
        let paused = false;
        c.addEventListener('pointerdown', () => { paused = true; });
        c.addEventListener('pointerup', () => { setTimeout(() => { paused = false; }, 3000); });
        c.addEventListener('touchstart', () => { paused = true; }, { passive: true });
        c.addEventListener('touchend', () => { setTimeout(() => { paused = false; }, 3000); });
        setInterval(() => {
          if (paused) return;
          const maxScroll = c.scrollWidth - c.clientWidth;
          if (c.scrollLeft >= maxScroll - 5) {
            c.scrollTo({ left: 0, behavior: 'smooth' });
          } else {
            c.scrollBy({ left: itemW, behavior: 'smooth' });
          }
        }, 3000);
      });
    });
  </script>
</BaseLayout>
